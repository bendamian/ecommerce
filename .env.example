# SECURITY WARNING: keep the secret key used in production secret!
# This key is used for cryptographic signing. Never share or expose it in public repositories.
SECRET_KEY=

# SECURITY WARNING: don't run with debug turned on in production!
# Set this to False in production to avoid exposing sensitive debug information.
DEBUG=

# Database Configuration
# These settings are used to connect to your database.
# Set the appropriate values based on your database provider (e.g., PostgreSQL, MySQL).
HOST=        # Database host, e.g., 'localhost' or an IP address
NAME=        # Name of your database
USER_ID=     # Database username
PASSWORD=    # Database password (keep this secret)

# Stripe Payment Integration
# These keys are used to interact with the Stripe API for payment processing.
STRIPE_PUBLIC_KEY =        # Public key for frontend use (safe to expose)
STRIPE_SECRET_KEY =        # Secret key for server-side use (keep this confidential)
STRIPE_WEBHOOK_SECRET =    # Used to verify Stripe webhook signatures (keep this secret)
